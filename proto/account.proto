syntax = "proto3";

package hello;
option go_package = "internal/protoc/hello";

message HelloReq {
  string msg = 1;
}

message HelloResp {
  string msg = 1;
}

service Tester {
  rpc SayHello(HelloReq) returns(HelloResp);
}